title = "ikea-symfonisk-gateway"

[extend]
useDefault = true

[[allowlists]]
description = "Allowlisted files"
files = [
  ".env.example",
  ".venv/.*",
  "__pycache__/.*",
]

[[allowlists]]
description = "Allow documentation examples with placeholder tokens"
regex = "Authorization: Bearer (your-token|<[^>]+>|\\*+)"
